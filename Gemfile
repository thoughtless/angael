source "http://rubygems.org"

# Specify your gem's dependencies in angael.gemspec
gemspec


group :development, :test do
  # TODO: Remove this line once this gem has been published on rubygems.org, 
  #       and put it in the gemspec file instead.
  gem 'rspec-process-mocks', :git => 'git://github.com/thoughtless/rspec-process-mocks.git'
end
